import 'package:flutter/material.dart';

class JoinChatroomPage extends StatelessWidget {
  const JoinChatroomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:
          Center(child: Text('Join Chatroom Page replaced by ChatroomPrePage')),
    );
  }
}
