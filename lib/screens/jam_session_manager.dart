import 'package:flutter/material.dart';

class JamSessionManager extends StatelessWidget {
  const JamSessionManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
